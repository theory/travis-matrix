language: perl
perl:
  - "5.28"
  - "5.26"
env: PGVERSION=system
script: perl -E 'say $ENV{PGVERSION}'
matrix:
  include:
    - env: PGVERSION=11
    - env: PGVERSION=10
jobs:
  include:
    stage: Special
    script: perl -E 'say $ENV{PGVERSION}'
